# 《Silent Reckoning·1987》版本开发与更新日志

> 本文档合并原 `0.2版本开发文档.docx`、`战棋0.3更新日志.md`，并持续补录后续修复记录。较新的版本记录优先于旧版本描述。

## 版本索引

| 版本 | 日期/阶段 | 核心定位 |
| --- | --- | --- |
| 0.5.6 | 2026-08-11 | 天气视觉：NoiseTexture2D 雾/雪层（边境晨雾+白色走廊），补齐已有 weather 系统的视觉 |
| 0.5.5 | 2026-08-11 | 程序化音效：Python 合成 14 个 SFX + SoundManager 接入（音效 0%→全覆盖） |
| 0.5.4 | 2026-08-11 | 教学引导：第1关新手分步教程（选中→移动/攻击→结束计划→演绎） |
| 0.5.3 | 2026-08-11 | 任务简报浮窗 + 战后旁白 + 北约坦克阵营区分图标 |
| 0.5.2 | 2026-08-11 | 九宫格视觉复核：02=履带IFV、12=轮式步战车；25兵种图标映射补全 |
| 0.5.1 | 2026-08-11 | 单位贴图接入：苏式粗犷风令牌替换字符渲染 |
| 0.5.0 | 2026-08-10 | 十关可运行框架、八套占位主题地图与连续战役 |
| 0.4.0 | 2026-08-10 | 第二关“铁路线防御”、工兵雷区、贷款与连续战役 |
| 0.3.2 | 2026-08-10 | 测试可信度、完整存档恢复、模块接口、输入与发布工程化 |
| 0.3.1 | 2026-08-10 | 第一关闭环修复、事件实装、场景编码修复与真实引擎验证 |
| 0.3 | 2026-08-06 | 主菜单、第一关场景、交互、暂停、卡牌与胜利进度 |
| 0.2 | 2026-07-30 至 2026-07-31 | 2.5D 地图接入、设计关卡替换测试地图、摄像机与阶段控制 |
| 0.1 | 初始设计阶段 | 十关战役、阵营、卡牌与剧情规则草案 |

## 战棋0.5.1 单位贴图接入

日期：2026-08-11

### 核心更新

- 从 `美术资产库/02苏式粗犷风/01_单位令牌.png` 九宫格切出 9 张单位图标（128×128、圆形遮罩），存入 `assets/units/`。
- `UnitRenderer.gd`：单位渲染由圆形+形状字符（◆■●）改为圆形阵营色环+贴图令牌（draw_texture_rect），保留生命条、士气点、朝向箭头、选中框。
- 贴图映射：步兵班→00、摩托化步兵/工兵→10、T-72B/M1A1→01、BMP-2/M2/M113→02、侦察→11、火炮/防空（BM-21/SA-13/ZSU-23/M109等）→12、AH-64→22。
- 图标 02（装甲车/步战）与 12（火炮/防空）为像素+视觉模型推断映射，待人工核对九宫格后微调。
- 验证：Godot 4.7.1 headless 三套测试全 PASS（25+14+61 checks），贴图成功导入。

### 待办

- ~~人工核对 01_单位令牌.png 第3格（02）与第6格（12）实际兵种，调整 UNIT_ICON_PATHS 映射。~~ → 已完成（0.5.2 视觉复核：02=履带IFV 原映射正确；12=轮式步战车，BM21/SA13 借用，无需按"02=坦克/12=侦察车"旧推断调整）。
- 后续可把 01_写实军事风/01_单位精灵表.png 作为可选第二套皮肤。

---

## 战棋0.5.2 单位贴图九宫格视觉复核与映射补全

日期：2026-08-11

### 核心更新

- 用 moonshot-v1-8k-vision-preview（0.6s/张便宜视觉模型）逐张复核 9 张单位令牌 + 九宫格总览，识别结论：
  - 00/10/21 = 步兵（单兵步枪/双人通信组/护目镜步枪手）
  - 01/20 = 主战坦克（01 锈红 / 20 灰绿）
  - 02 = 履带式步战车 IFV（小口径机关炮炮塔）
  - 12 = 轮式步战车/APC（8×8 轮式底盘）
  - 11 = 侦察兵（举望远镜，非侦察车）
  - 22 = 攻击直升机（主旋翼+短翼挂架）
- 关键结论：0.5.1 推断的 02/12 映射无需按"02=坦克/12=侦察车"修改——02 本就是 IFV（BMP-2/M2 映射正确），12 为轮式步战车；9 图无火箭炮/防空专用图，BM-21（轮式底盘）/SA-13 借用 12 最接近。
- 映射微调（按视觉分类就近借用，全 25 兵种均有图标）：
  - MECH_INFANTRY 10→21（护目镜步兵图，不再与摩托化步兵共用）
  - ATGM_TEAM 12→21（反坦克导弹组是步兵）
  - BRDM2_RECON 11→12（BRDM-2 轮式侦察车，用轮式装甲图）
  - ZSU23_AA / GVOZDIKA / M109 / M901 12→02（履带底盘，用履带 IFV 图）
- UnitRenderer.gd 注释更新为复核后的九宫格构成。
- 验证：Godot 4.7.1 headless 三套测试全 PASS（25+14+61 checks）。

### 待办

- 20（灰绿坦克）已启用为北约 M1A1 图标（与华约 T-72B 锈红 01 区分）。
- 后续可把 01_写实军事风/01_单位精灵表.png 作为可选第二套皮肤。

---

## 战棋0.5.3 任务简报浮窗 + 战后旁白 + 坦克阵营区分

日期：2026-08-11

### 核心更新

- **任务简报浮窗**（MainScene._show_briefing_panel）：LEVEL_INTRO 阶段弹出居中浮窗，显示第 N 关《关卡名》、briefing 正文、主任务/胜利/失败条件、回合限制、情报（intel_a），底部"开始行动"按钮。
- **确认机制**（GameManager）：_start_level_intro 由 3 秒自动跳改为等待玩家确认（intro_confirmed 信号 + 60 秒兜底），confirm_intro() 供按钮调用；防 lambda 按值捕获坑，用成员变量 _intro_confirm_requested。
- **战后旁白**（game over 面板）：结算面板原因下方新增旁白行（outro_narration，金色斜体风格）。10 关全部补齐文案；第 1 关错位的"铁轨/车厢"文案移回第 2 关《铁路线防御》，第 1 关换写边境晨雾专属旁白。
- **坦克阵营区分**：M1A1_TANK → unit_brutalist_20.png（灰绿西式坦克），T-72B 保持 01（锈红），华约/北约坦克不再共用图标。
- 测试适配：LEVEL_INTRO 等待确认后，level_02 与 campaign 测试需模拟点击（GameManager.confirm_intro()）；临时验证脚本确认浮窗创建/内容/按钮/关闭/状态切换全部正确后删除。
- 验证：Godot 4.7.1 headless 三套测试全 PASS（25+14+61 checks）+ 简报专项验证 PASS。

### 待办

- 教学引导（新手教程）、音效（0%）、第3-10关主题地形、天气视觉（雾）、ARCHITECTURE.md 文档滞后。
- 后续可把 01_写实军事风/01_单位精灵表.png 作为可选第二套皮肤。

---

## 战棋0.5.4 教学引导（第1关新手教程）

日期：2026-08-11

### 核心更新

- **TutorialManager**（scripts/ui/TutorialManager.gd）：第 1 关首次进入时底部提示条分步引导核心操作闭环。
- 4 步流程：① 点击蓝色（华约）单位选中 → ② 蓝格规划移动/红敌攻击 → ③ 继续下令并『结束计划』 → ④ 观看沙盘演绎（可跳过）。第 2 回合计划开始后自动完成。
- 事件驱动：MainScene 在选中/攻击提交/移动确认/进入演绎等 5 处调用 notify()；满足条件才推进，乱序操作自动跳过中间提示（无回退）。
- 提示条：底部居中 680×80 Panel（简报同风格边框）+ 自动换行文案 + "跳过教学"按钮（右下角）。
- 完成/跳过 → GameManager.tutorial_done = true（本次运行内第 1 关不再重复引导，内存级不落盘）。
- 验证：Godot 4.7.1 headless 三套测试全 PASS（25+14+61）+ 教学专项验证 PASS（创建/乱序容错/完整链路/完成隐藏/不重复创建）。

### 待办

- 教学引导已闭环；剩余：音效（0%）、第3-10关主题地形、天气视觉（雾）、ARCHITECTURE.md 文档滞后。
- 后续可把 01_写实军事风/01_单位精灵表.png 作为可选第二套皮肤。

---

## 战棋0.5.5 程序化音效（0% → 核心交互全覆盖）

日期：2026-08-11

### 核心更新

- **Python 合成 14 个音效**（tools/synthesize_sfx.py，numpy 离线生成 44100Hz 16bit wav，重跑即可整体重生成，共 744KB）：
  - 交互：ui_click / unit_select（五度双音）/ move_order（双脚步）/ card_use（滑音）/ loan_coin（金币交替双音）
  - 战斗：gunshot（三连发）/ tank_fire（低频砰+瞬态）/ hit / explosion（白噪长衰减+轰鸣滑降）/ morale_break（锯齿下滑）
  - 流程：round_plan（三连音）/ round_exec（双鼓点+金属叮）/ victory（C-E-G 上行）/ defeat（A-E-A 下行）
- **SoundManager**（scripts/audio/SoundManager.gd，autoload）：8 个 AudioStreamPlayer 池循环复用，流按需加载缓存，播放完释放引用，退出时清理；`SoundManager.play("explosion", -2.0)` 全局调用。
- **挂钩 11 处**（MainScene）：选中/移动确认/攻击提交/攻击执行/命中/摧毁/士气崩溃/回合计划/回合演绎/卡牌使用/贷款/胜利/失败。
- 修复：play() 未知键先查 SFX 字典（原 SCRIPT ERROR）；numpy 广播错误（短数组 += 改为偏移切片）。
- 已知噪音：Godot headless 无音频设备时退出报 "resources still in use"（AudioServer 持有已播放流），窗口模式不出现，不影响测试判定。
- 验证：三套测试全 PASS（25+14+61）+ 音效专项验证 PASS（14 流加载播放/池复用/未知键静默）。

### 待办

- 音效已闭环；剩余：第3-10关主题地形、无线电对话、ARCHITECTURE.md 文档滞后。
- 后续可把 01_写实军事风/01_单位精灵表.png 作为可选第二套皮肤。

---

## 战棋0.5.6 天气视觉（雾/雪层）

日期：2026-08-11

### 核心更新

- **WeatherLayer**（scripts/ui/WeatherLayer.gd，CanvasLayer layer=70）：全屏半透明底色 + 3 团 FastNoiseLite 噪声雾（运行时生成，零外部素材），sin 波缓慢漂移（周期 20~32s）。
- 发现 LevelData/MainScene **已有完整天气数据与逻辑**（weather 字段、weather_vision_penalty 视野惩罚、_apply_level_start_effects、战报提示）——缺的只有视觉层，本次补齐：第 1 关边境晨雾 fog、白色走廊 snow 原本已配置。
- 修正过程：曾误加重复 weather 赋值、并把 snow 错加到"全频段窒息"关（EMI 100% 不该有雪），已还原。
- 踩坑：`var x := (load(...) as GDScript).new()` 类型推断为 Variant 触发 "Warning treated as error" 导致 MainScene 编译失败——改用无标注 `var x = ...`（与 TutorialManager 创建一致）。
- 验证：三套测试全 PASS（25+14+61）+ 天气专项验证 PASS（fog 关 3 团雾 / clear 无雾 / snow 有雾）。

### 待办

- 天气视觉已闭环；剩余：第3-10关主题地形、无线电对话、ARCHITECTURE.md 文档滞后。
- 后续可把 01_写实军事风/01_单位精灵表.png 作为可选第二套皮肤。

---

## 战棋0.5.0 十关框架更新

日期：2026-08-10

### 核心更新

- 第3至第10关新增独立场景，全部可从主菜单选择并可通过结算连续推进。
- 新增通用20×12程序化地图构建器，提供城市、密林、防线、河谷、全频干扰、雪地、铁路和终局废墟八种占位主题。
- 为后八关补齐出生点、任务目标、任务简报及缺失的北约初始部队和指挥单位。
- 接通“洪水”EMI、未知接触、断桥抉择、浮桥工兵、大雪视野等关卡事件框架。
- 修复第10关初始EMI未按设计从100%开始、第二关以外天气未应用视野惩罚、VP总数显示固定为3的问题。
- 新增61项十关框架测试，并完整启动第10关验证25支单位、AI、EMI和指挥单位绑定。

---

## 战棋0.4.0 第二关更新

日期：2026-08-10

### 核心更新

- 新增20×12第二关“铁路线防御”场景：铁路、河道、三处通路VP及防御建筑。
- 实装15支初始单位、稳步推进AI、10回合结算、任务简报与情报。
- 实装工兵排雷与普通单位触雷：地雷单次触发，造成30%生命伤害并停止移动。
- 新增指挥贷款按钮，修复贷款立即抽牌被下回合惩罚错误抵消的问题。
- 第6回合授予“预备队投入”，第5回合EMI升至15%，并接入北约工兵、AH-64与难民车队事件。
- 第一关胜利结算新增“进入下一关”，主菜单关卡选择开放第二关。
- 新增第二关14项专用回归测试；第一关25项测试继续通过。

---

## 战棋0.3.2 工程化与存档更新

日期：2026-08-10

### 版本定位

0.3.2 在不改变第一关核心规则的前提下，修复回归测试可能假通过的问题，并补齐可实际恢复战场的存档流程、模块公共接口和发布配置。

### 核心更新

- 冒烟测试改为累计失败后统一退出，避免失败后仍打印 PASS；检查项扩展至 21 项。
- 新增 `tools/run_tests.py`，同时检查进程退出码、Godot 脚本错误和测试结果标记。
- 暂停界面新增保存功能，主菜单新增“继续游戏”。
- 存档现在包含战役、回合、EMI、士气、卡牌和所有存活单位的生命、弹药、坐标、视野及行动状态。
- 修复读取存档时关卡初始化覆盖恢复数据、JSON 普通数组无法赋给类型数组等问题。
- 为胜利点统计和结束游戏增加公共接口，停止跨模块调用 `VictoryManager` 私有方法。
- 摄像机、暂停与全屏操作迁移到 `InputMap` 动作入口，为后续键位设置预留接口。
- 发布预设改为导出完整运行时资源，同时排除测试、工具、文档和素材源图，并关闭发布版控制台窗口；补充 `.gitattributes` 和构建目录忽略规则。
- README 与架构文档更新为实际的四方向等距方格架构。
- 工程版本更新为 `0.3.2 build 3`。

### 验证结果

- Godot 4.7.1 编辑器无头加载通过。
- 第一关完整初始化通过。
- 存档写入、读取、单位生命值与回合恢复通过。
- 21 项冒烟检查通过，未发现脚本编译错误。
- 完整资源 PCK 打包通过（约 2.61 MB），并已通过 `--main-pack` 启动验证；Windows EXE 仍需安装对应版本官方导出模板。

---

## 战棋0.3.1 修复与可玩性验证

日期：2026-08-10

### 版本定位

0.3.1 是第一关可玩闭环修复版本，重点解决重开状态、胜负判定、回合事件和场景解析问题，并建立可重复的 Godot 4.7.1 冒烟测试。

### 核心修复

- 新增统一关卡状态重置：回合、双方指令、事件、处理记录、单位 ID 与胜负状态不再跨局残留。
- 指挥中心胜负改为绑定开局驻守单位 ID，指挥单位移动或死亡后仍能可靠判定。
- 第一关事件接入实际逻辑：晨雾、雾散、AH-64 增援、EMI 上升、未知接触和炮兵就绪。
- 修复 32 个建筑场景文件因 UTF-8 BOM 导致的 Godot 解析失败。
- 新增 Windows Desktop 导出预设、运行说明和自动冒烟测试。
- 工程版本更新为 `0.3.1 build 2`。

### 验证结果

- Godot 4.7.1 首次导入通过。
- 主菜单无界面启动通过。
- 第一关地图、建筑、18 个初始单位、7 张起始手牌和 AI 规划加载通过。
- 雾效恢复、增援生成、EMI 事件、重置和指挥单位胜负测试通过。
- 已知范围内脚本与场景解析错误为 0。

---

## 战棋0.3 更新日志

日期：2026-08-06

### 版本定位

战棋0.3 是在战棋0.2.1 基础上的首个可菜单化试玩版本，重点补齐第一关场景内容、基础交互、卡牌操作、暂停与胜利进度显示，并整理中文文本编码问题。

### 场景与地图

- 填充第一关场景 `res://scenes/levels/level_01.tscn`。
- 基于 `assets/terrain/isometric_tileset.png` 设计草地、道路、森林、水域、泥地、建筑占地等不同地形。
- 将全局地图尺寸从 `20x15` 扩展为 `40x45`。
- 调整双方出生点布局，确保双方出生点距离超过 30 个单位。
- 添加并配置第一关双方基础兵种。
- 完成第一关加载与运行验证。

### 视角与输入

- 修复窗口缩放时场景不随窗口缩放的问题。
- 修复全屏显示异常，支持窗口正确全屏。
- 修复鼠标与键盘输入无效的问题。
- 为摄像头添加鼠标滚轮缩放功能。
- 为摄像头添加鼠标左键拖动地图功能。
- 添加鼠标悬停地块高亮提示。

### 单位、建筑与卡牌交互

- 鼠标悬停单位或建筑时显示名称。
- 为我方建筑和敌方建筑添加不同颜色高亮区分。
- 使用卡牌时，根据卡牌类型用不同颜色高亮作用范围。
- 单位移动增加确认步骤。
- 修复卡牌描述文本超出卡牌范围的问题，改为多行显示并限制在卡牌内。
- 添加卡牌栏展开/收起按钮，减少对场景的遮挡。
- 修复战报和控制台中的弃牌文本显示。

### 暂停、胜利条件与进度

- 添加暂停按钮。
- 暂停时仍可查看地图局势、单位和建筑，但不能操作。
- 添加胜利条件和完成进度显示。
- 将胜利条件/进度改为类似战报的半透明文本框样式，并缩小字号。

### 开始界面与关卡选择

- 新增开始界面。
- 新增关卡选择界面。
- 统一使用 `res://封面图.png` 作为开始界面和关卡选择界面的背景。
- 将按钮放置在窗口中下部，避免遮挡封面文字。
- 按钮文字改为白色加粗。
- 修复封面图显示方式，改为完整等比例显示，不再裁切。
- 项目启动入口改为菜单场景 `res://scenes/MainMenu.tscn`。

### 文本与编码

- 检索并修复项目内 UTF-8 被错误当成 GBK 读取后保存造成的中文乱码。
- 修复 `scripts/MainScene.gd` 中多处乱码注释。
- 修复 `scripts/ui/CardUI.gd` 中手牌 UI 乱码注释。
- 修复 `scripts/levels/LevelDatabase.gd` 中关卡事件文本：
  - `未知接触?` 调整为 `未知接触点`。

### 仓库整理

- 删除旧版本目录：`战棋`、`战棋0.1`、`战棋0.2`、`战棋0.2.1`。
- 将本地项目 `战棋0.2.1` 更名为 `战棋0.3`。
- 将 `战棋0.3` 作为当前主版本同步到 GitHub 仓库。

---

## 战棋0.2 开发记录

内部开发记录

Silent Reckoning·1987

暂定版本 V0.2｜两日开发文档

统计周期：2026年7月30日—7月31日

项目路径：C:\Users\Lenovo\Desktop\0.2\战棋0.2

引擎环境：Godot 4.7.1 stable｜模式：单人 PVE 原型

本阶段结论  V0.2 已从脚本绘制的平面测试地图，转向可由设计师在 Godot 场景中编辑的 2.5D 等距地图；地图、单位、标记与悬停坐标已共用同一逻辑参考系，并补齐摄像机移动、缩放和阶段提前结束功能。

## 文档目的

记录过去两天完成的设计与开发工作、涉及文件、行为变化、验证结果和后续风险。本文中的“V0.2”指游戏工程暂定版本；《关卡细化设计》V1.1 仅表示设计文档修订号，两者不可混用。

### 当前可运行状态

- 主场景可加载设计师绘制的 level_01.tscn，并从 Terrain 图层提取运行时地图。

- 当前第一关运行时地图尺寸为 28×21，场景原始 TileMap 起点为 (-10,-17)，进入战斗后被规范化为逻辑坐标。

- 华约与北约单位、卡牌系统、AI、胜负判定和回合系统能够随第一关正常初始化。

## 1. 两日工作总览

| 日期 | 工作主题 | 完成内容 | 状态 |
| --- | --- | --- | --- |
| 7月30日 | 地图基础改造 | 导入等距像素地块；建立 TileSet、TileMapLayer 和关卡场景；统一地图坐标与 Marker；让单位渲染适配等距地图。 | 基础完成 |
| 7月31日 | 玩法与交互完善 | 加载设计关卡；动态读取地图范围与地块数据；加入摄像机移动/缩放；加入计划结束与演绎跳过。 | 可运行 |
| 7月31日 | 设计文档同步 | 把地图、坐标、高低差、关卡制作方法和第一关参考布局同步到《关卡细化设计》修订版。 | 已输出 |

### 1.1 版本范围

V0.2 的重点不是扩充兵种或制作完整十关战役，而是建立“设计师能够画地图，程序能够读取地图，战斗逻辑能够使用地图”的最小工作链。当前只以第一关验证整条链路。

### 1.2 核心工作链

1. 设计师在 level_01.tscn 的 Terrain 图层绘制地块，并在 TileSet 中填写地块自定义数据。

1. MainScene 加载关卡场景，通过 Terrain.get_used_rect() 获取实际地图范围。

1. 场景坐标被规范化为从 (0,0) 开始的内部逻辑坐标，并交给 GridManager、单位渲染器和路径系统。

1. 玩家界面把内部坐标加 1，显示为纯数字 (X,Y)，不再使用字母纵坐标。

1. 修改关卡后保存场景并重新运行，调试地图随已保存的 level_01.tscn 同步。

### 1.3 本阶段未扩大范围

- Decorations 装饰层已建立，但暂时不绘制，等待统一美术素材。

- 没有在本阶段扩充新兵种、司令技能或完整多关卡场景。

- 没有重新制作战斗数值；本阶段主要解决地图生产与交互基础。

## 2. 7月30日：2.5D地图与坐标基础改造

### 2.1 等距地图素材接入

导入 isometric_tileset.png，并建立 isometric_terrain.tres。TileSet 使用等距四边形地块，当前地图视觉由草地、土路、石地和水域等地块组合。旧的纯色脚本网格不再承担正式地形表现。

统一方向  地块确定为 2.5D 等距四边形，不再继续生成或维护六边形地图方案。

### 2.2 关卡场景结构

- Level01：第一关场景根节点。

- Terrain（地块）：主要 TileMapLayer，决定地图边界与战斗地形。

- Decorations（装饰）：独立 TileMapLayer，仅放置视觉装饰，目前允许为空。

- Markers：存放出生点、胜利点和指挥中心等 GridMarker2D 标记。

### 2.3 地块数据化

| 自定义字段 | 类型 | 用途 |
| --- | --- | --- |
| terrain_type | String | 草地、土壤、石地、水域、道路等地形类型 |
| height | Int | 五级高度 -2、-1、0、+1、+2 |
| move_cost | Float | 进入地块所需移动消耗 |
| passable | Bool | 地块是否允许通行 |
| concealment | Float | 隐蔽修正，用于视野与命中相关计算 |

### 2.4 坐标参考系统一

GridMarker2D 不再依赖手动拖动后的像素位置，而是保存 grid_coordinate。编辑器和运行时根据 Terrain 的 used_rect 起点，把地图坐标自动换算为全局像素位置。这样修改地图原点或扩展地图边界时，标记能够跟随地块更新。

- 内部坐标：从 (0,0) 开始，用于脚本、寻路和单位状态。

- 玩家坐标：从 (1,1) 开始，统一显示为数字 (X,Y)。

- 当前场景中的 WPSpawn_01 内部坐标为 (3,6)，玩家显示应为 (4,7)。

## 3. 7月31日：设计关卡接入与交互完善

### 3.1 使用设计师场景替代脚本测试地图

MainScene 现在直接加载 res://scenes/levels/level_01.tscn。程序查找 Terrain TileMapLayer，读取全部已使用单元格，并把地图大小、地形属性与标记传递给运行时系统。LevelDatabase 仍保存关卡单位、事件和胜负数据，但不再作为第一关视觉地形的来源。

- 地图尺寸由 get_used_rect() 动态产生，不再固定为 20×15。

- 当前读取结果为 28×21，原始起点 (-10,-17) 被转换为逻辑起点 (0,0)。

- Terrain 的 terrain_type、height、move_cost、passable、concealment 会进入 GridManager。

### 3.2 地图、单位与选择系统同步

HexGridRenderer 与 UnitRenderer 增加 source_tilemap 和 used_rect 参考。地图画面、单位位置、鼠标点击与悬停坐标使用同一套等距换算，解决此前重复绘制两份地图、上方地图不能移动以及单位无法选中的问题。

### 3.3 摄像机操作

- W/A/S/D：以约 420 像素/秒移动地图视图。

- 鼠标滚轮：缩放地图；代码限制有效缩放区间，避免地图缩到不可操作或过度放大。

- 窗口尺寸变化时重新计算地图适配比例和偏移。

- 地图图层与单位渲染器同时接收摄像机偏移和缩放，维持点击坐标一致。

### 3.4 悬停坐标显示

计划阶段读取鼠标所在地图格，并在左上角状态区域显示玩家坐标。显示格式统一为数字 (X,Y)，地图宽度不受 26 格或英文字母数量限制。

## 4. 回合阶段控制修改

### 4.1 提前结束计划阶段

新增“提前结束计划”按钮。计划阶段仍保留 60 秒上限，但玩家完成命令后可以立即锁定双方命令并进入沙盘演绎。按钮和倒计时统一调用 _finish_planning()，避免出现两套状态切换流程。

### 4.2 跳过演绎阶段

进入演绎阶段后，同一按钮切换为“跳过演绎”。点击后将 execution_skip_requested 设为 true。如果所有动作已经完成，立即进入回合结算；如果仍有单位移动或攻击，则等待 TurnManager 报告 execution_actions_finished，再安全结束演绎。

安全原则  跳过演绎只跳过剩余等待时间，不会在移动、攻击或伤害结算中途强制切断动作。

### 4.3 界面状态

| 当前阶段 | 按钮文字 | 行为 |
| --- | --- | --- |
| 计划阶段 | 提前结束计划 | 锁定命令并进入演绎 |
| 演绎阶段 | 跳过演绎 | 动作完成后进入结算 |
| 其他阶段 | 隐藏 | 避免误触发状态切换 |

### 4.4 当前时间规则

- 计划阶段：最长 60 秒，可提前结束。

- 演绎阶段：最长 30 秒，可请求跳过剩余等待。

- 两段时间均为上限，不再要求玩家必须等待计时器归零。

## 5. 主要文件修改记录

| 文件 | 本阶段职责/修改 |
| --- | --- |
| scripts/MainScene.gd | 关卡加载、地图提取、摄像机、悬停坐标、阶段按钮、输入处理 |
| scripts/core/GameManager.gd | 运行时地图注入；计划提前结束；演绎安全跳过 |
| scripts/core/TurnManager.gd | 提供演绎动作完成状态，配合安全结算 |
| scripts/core/GridManager.gd | 等距地图尺寸、地块属性和坐标转换 |
| scripts/levels/GridMarker2D.gd | 地图坐标驱动 Marker 位置；玩家坐标输出 |
| scripts/ui/HexGridRenderer.gd | 读取设计 TileMap，统一地图偏移与缩放 |
| scripts/ui/UnitRenderer.gd | 单位位置改为参考设计 TileMap |
| scenes/MainScene.tscn | 主场景节点与渲染结构调整 |
| scenes/levels/level_01.tscn | 第一关 Terrain、Decorations、Markers 与实际地块 |
| resources/tilesets/isometric_terrain.tres | 等距图集、Terrain Set 与五个自定义数据层 |
| assets/terrain/isometric_tileset.png | 2.5D 等距像素地块素材 |
| project.godot | 项目输入、窗口与主场景配置 |

### 5.1 命名上的过渡状态

部分脚本仍使用 HexGridRenderer、HexPathfinding 等旧名称，但实际地图方向已经改为等距四边形。本阶段优先保证功能连续，没有立即大范围重命名，避免引发引用丢失；后续应集中清理。

### 5.2 设计文档与美术参考

《Silent_Reckoning·1987_关卡细化设计》已输出 V1.1 地图同步版。修改包括：场景化地图制作、纯数字坐标、五级高低差、Terrain/Decorations/Markers 分层、阶段提前结束，以及第一关 2.5D 地图布局参考图。该 V1.1 是文档修订号，不改变游戏工程暂定版本 V0.2。

## 6. 验证结果

### 6.1 运行验证

使用 Godot 4.7.1 stable 进行无窗口启动检查。项目能够完成脚本加载、子系统初始化、第一关场景读取、地图初始化、双方单位生成和起始手牌抽取。

| 检查项 | 结果 | 证据 |
| --- | --- | --- |
| 设计关卡加载 | 通过 | 读取 level_01.tscn |
| 地图范围 | 通过 | 28×21，原始起点 (-10,-17) |
| 核心子系统 | 通过 | AI、战斗、移动、卡牌、士气、EMI、胜负判定均初始化 |
| 第一关单位 | 通过 | 华约 6 个单位，北约单位正常创建 |
| 起始卡牌 | 通过 | 起手调整为 7 张 |

### 6.2 本次验证边界

- 无窗口测试证明项目能够启动和初始化，但不能替代人工检查鼠标选择、WASD、滚轮与按钮视觉反馈。

- 尚未完成完整 8 回合自动对局，也未覆盖所有卡牌和随机事件组合。

- 地图碰撞、不同兵种涉水和五级高度收益仍需关卡内逐项验收。

## 7. 已知问题与后续建议

| 问题 | 说明 | 优先级 |
| --- | --- | --- |
| 版本号不一致 | project.godot 仍显示 1.0，GameManager 仍打印 v1.0.0；应统一改为 0.2.0，并明确 build 编号。 | 高 |
| 旧 Hex 命名残留 | HexGridRenderer、HexPathfinding 等名称与当前等距四边形方向不一致。 | 中 |
| TileSet 数据清理 | 部分 terrain_type 文本含多余引号或空格，当前由脚本清洗；应在资源中统一为标准值。 | 中 |
| Marker 数量不足 | level_01.tscn 当前只看到 WPSpawn_01；需补齐北约出生点、胜利点和双方指挥中心。 | 高 |
| Decorations 为空 | 等待统一素材后再绘制，但必须保持装饰不直接决定通行逻辑。 | 低 |
| 地图规则未完全落地 | 高度 -2 至 +2 已有字段，仍需把视野、射程、隐蔽和移动代价逐项接入并测试。 | 高 |
| 缺少自动测试 | 当前主要依靠运行日志和人工操作，建议增加坐标转换、地图提取和阶段切换测试。 | 中 |

### 7.1 下一开发日建议顺序

- 先把项目显示版本统一为 V0.2.0，并建立可回滚的版本备份或 Git 提交。

- 在第一关补齐全部 GridMarker2D，并确认玩家坐标与单位初始坐标一致。

- 逐格校正 terrain_type、height、move_cost、passable、concealment，清除多余引号与空值。

- 人工验证单位选择、移动、悬停坐标、WASD、滚轮、计划结束和演绎跳过。

- 将第一关高地、洼地、道路和水域的实际效果接入视野、攻击距离和路径成本。

- 确认第一关可从开始完整运行至胜利或失败，再进入新兵种、美术装饰和第二关制作。

V0.2 验收定义  第一关地图能够由设计师独立修改并保存；运行后地图、Marker、单位、悬停坐标与点击选择一致；摄像机可操作；计划和演绎可提前结束；至少完成一局从开场到胜负结算的人工测试。
